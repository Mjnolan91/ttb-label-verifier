# Ralph Progress Log
Project: TTB Label Verifier
---
US-001 (scaffold): Next 16 (App Router, Turbopack) + React 19.2 + TS (strict) + Tailwind v4 +
  Vitest 4. Home page is one centered card (src/app/page.tsx); title lives once in
  src/app/constants.ts (APP_TITLE) and feeds both the UI and <title>, unit-tested in app.test.ts.
  Scaffold cleanly absorbed the pre-seeded src/domain tests: `npm test` = 5 files / 31 tests green.
GOTCHAS for next iterations: (1) `next lint` was REMOVED in Next 16 — lint is `eslint .` via flat
  config eslint.config.mjs (eslint-config-next/core-web-vitals + /typescript). ESLint pinned ^9.34
  (config-next peer wants >=9; 10 risks typescript-eslint compat). (2) TypeScript pinned ^5.9.3 on
  purpose (avoid TS 6.0 tsconfig-removal + typescript-eslint range surprises). (3) Tailwind v4:
  @tailwindcss/postcss in postcss.config.mjs + `@import "tailwindcss"` in globals.css; NO
  tailwind.config / autoprefixer needed. (4) `next build` auto-rewrites tsconfig (jsx->react-jsx)
  and next-env.d.ts (adds import of .next/types/routes.d.ts) — keep those; cold `tsc --noEmit` still
  passes with .next absent, so the loop's typecheck is fresh-checkout safe. (5) Tests scoped to
  src/** + eval/** (vitest.config.ts), env=node; switch UI test files to jsdom per-file later.
US-002 (domain integration): Confirmed the pre-seeded src/domain exports everything later stories
  need (ClaimedFields/ExtractedFields with per-field confidence; CANONICAL_GOVERNMENT_WARNING; full
  tolerance matrix + selectToleranceFor; proof helpers) — NOTHING in the domain was reworded/retuned.
  Wired it in as the single source of truth: app (src/app/page.tsx) and a new
  src/domain/integration.test.ts both import via the "@/domain" barrel/alias. Verified no duplicate
  copy of the warning/tolerance CONSTANTS exists in code (only warning.test.ts keeps an intentional
  independent verbatim copy). `npm test` = 6 files / 34 tests; typecheck/lint/build all green; the
  home page renders the canonical warning straight from the domain constant (bold caps prefix).
NOTES for next iterations: (a) `@/domain` resolves in BOTH tsconfig paths and the Vitest alias — use
  it everywhere; never re-declare domain constants. (b) ExtractedFields already carries the tri-state
  warningPrefixIsBold (true|false|null) and warningPrefixIsAllCaps — US-004 reads these flags rather
  than re-deriving format from raw text. (c) Harmless favicon.ico 404 in the browser console is the
  only console error; add an app icon during a UI story if a story needs a clean console.
US-003 (extraction): src/extraction/ = VisionProvider interface + ImageInput ({filename, data?,
  contentType?}; mock uses filename ONLY) + MockVisionProvider + getVisionProvider(env) factory
  (default mock; llm/ocr throw clean "not wired yet" errors — no network). Mock imports
  eval/fixtures/cases.json at BUILD time (resolveJsonModule) and maps each `extracted` block to
  ExtractedFields; unknown filename -> deterministic zero-confidence, no-value result. `npm test`
  = 7 files / 44 tests. KEY DECISION for US-004: extracted/claimed alcohol flows as RAW TEXT via
  the new ExtractedFields.alcoholContentText (e.g. "45% Alc./Vol. (90 Proof)"); the COMPARATOR
  parses it (extraction stays raw). US-004 must add ClaimedFields.alcoholContentText + the parser.
  Empty warningText is preserved verbatim: "" at HIGH confidence = confidently-absent (US-004 fail);
  "" / all-low confidence = unreadable (review/re-upload) — the mock keeps these two paths distinct.
US-004 (comparator): src/compare/ = compareBrand/compareAlcohol/compareWarning (pure, return
  {status,claimed,extracted,reason}) + helpers (text normalize/Levenshtein similarity; parseAlcoholText;
  resolveBeverageClass class-text->enum; isLowOrReducedAlcoholClaim). `npm test` = 10 files / 90 tests.
  DESIGN: comparators are VALUE-BASED and confidence-AGNOSTIC on purpose — low-confidence->review is a
  SEPARATE gate (US-011) applied over these verdicts in the verify orchestration. Brand: exact-after-
  normalize=pass, >=0.8 similar=review, else fail. Warning: wording compared case-insensitively, prefix
  caps/bold read from the EXTRACTED FLAGS (not re-derived), <0.5% ABV exempt via isWarningRequired.
  Alcohol: class SELECTS tolerance via selectToleranceFor; boundary clamps enforced HERE (wine 14% per
  4.36(c); malt 0.5% floor & 2.5% low/reduced cap per 7.65) — constants MALT_ABV_FLOOR/CAP live in the
  comparator, NOT domain. For US-005: build verifyLabel = getVisionProvider().extract -> the 3 compares
  -> overall (reject if any fail; else review if any review; else approve). compareAlcohol takes
  claimedClass text (resolve to enum) or an explicit beverageClass.
US-005 (API): POST /api/verify (src/app/api/verify/route.ts) parses multipart form (image + brand,
  alcoholContent, classType?, netContents?), validates (missing image/brand/alcoholContent -> 400),
  runs getVisionProvider().extract -> verifyLabel -> JSON {provider, claimed, extracted, result}.
  Uses web-standard Request/Response.json (NOT next/server) so the handler imports + runs directly in
  Vitest. New: src/compare/verify.ts (verifyLabel + overallVerdict + VerifyResult/OverallVerdict) and
  ClaimedFields.alcoholContentText (additive). `npm test` = 12 files / 101 tests; build OK (/api/verify
  is a dynamic route). NOTE: verifyLabel is still value-based (unreadable fixture would over-reject
  until the US-011 confidence gate). US-006 (upload UI) + US-007 (result UI) consume this JSON shape.
US-006 (upload UI): src/app/VerifyForm.tsx ("use client") = accessible drag-drop + file picker + labeled
  inputs (brand/alcohol required, classType/netContents optional) -> POST /api/verify -> shows a MINIMAL
  text result (US-007 replaces with announced color+icon cards). page.tsx hosts it + keeps @/domain wired
  via a <details> warning reference. A11y verified in-browser (Playwright): logical tab order (image->brand->
  alcohol->class->net->Verify->summary), visible focus ring (:focus-visible box-shadow), error path uses
  role=alert linked to the button via aria-describedby + moves focus to the file input. Shared response
  type in src/app/api/verify/contract.ts (VerifyApiResponse/Error). `npm test` still 101 (UI = browser-
  verified, no jsdom). GOTCHA: sr-only file input can't be clicked (label intercepts) — in Playwright set
  files via DataTransfer + dispatch 'change'; the mock only needs the FILENAME so a dummy File works.
US-007 (result UI): src/app/ResultView.tsx = overall banner (Approve/Needs review/Reject) + 3 per-field
  cards. Status is color + ICON + TEXT BADGE (PASS/REVIEW/FAIL), never color alone (WCAG 1.4.1). Colors
  are dark-text-on-light-tint (green/amber/red 50 bg, 900 text) for >=4.5:1; badges white-on-700. Region
  is role=status aria-live=polite; VerifyForm focuses the result <h2> (tabIndex -1) on completion. Claimed
  vs extracted shown side-by-side (sm:grid-cols-2), long warning text line-clamp-3. Browser-verified all
  three verdicts (approve/review/reject). `npm test` still 101 (UI browser-verified). Tailwind v4 line-clamp-3
  works out of the box. Favicon 404 is still the only console error.
