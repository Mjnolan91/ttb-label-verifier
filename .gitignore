# dependencies
node_modules/

# next.js / build
.next/
out/
dist/
build/
# auto-generated by `next dev`/`next build`; oscillates between .next/dev/types and
# .next/types route imports, so we don't track it. Cold `tsc --noEmit` passes without it.
next-env.d.ts

# env / secrets
.env
.env.*
!.env.example

# test / coverage
coverage/

# typescript incremental build cache
*.tsbuildinfo

# os / editor
.DS_Store
*.log

# playwright mcp / local verification artifacts
.playwright-mcp/
us0*-*.png
